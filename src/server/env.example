# =============================================================================
# ENVIRONMENT VARIABLES FOR CHOUIEUR EXPRESS BACKEND
# =============================================================================
# 
# This file contains environment variables for the backend server.
# Copy this file to .env and update the values for your environment.
# Never commit .env to version control - it contains sensitive information.
# 
# GOOGLE SHEETS CONFIGURATION:
# =============================
# GOOGLE_SHEETS_ID: The ID of your Google Sheet
# GOOGLE_SERVICE_ACCOUNT_EMAIL: Service account email from Google Cloud Console
# GOOGLE_PRIVATE_KEY: Service account private key (with \n for newlines)
# 
# SETUP INSTRUCTIONS:
# ===================
# 1. Create a Google Cloud Project
# 2. Enable Google Sheets API
# 3. Create a Service Account
# 4. Download the service account JSON key
# 5. Share your Google Sheet with the service account email
# 6. Set the environment variables below
# 
# GOOGLE SHEETS SETUP:
# ====================
# 1. Create a Google Sheet with the following tabs:
#    - Orders (with columns: id, userId, customerName, customerPhone, customerAddress, items, total, status, createdAt, updatedAt)
#    - MenuItems (with columns: id, name, category, price, megaPrice, description, imageId, isActive, createdAt, updatedAt)
#    - Users (with columns: id, firebaseUid, email, name, role, createdAt, updatedAt)
# 2. Share the sheet with your service account email
# 3. Get the sheet ID from the URL: https://docs.google.com/spreadsheets/d/SHEET_ID/edit

# Google Sheets Configuration
GOOGLE_SHEETS_ID=your_google_sheet_id_here
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# PORT: Server port (Render will set this automatically in production)
PORT=5000

# NODE_ENV: Environment mode
NODE_ENV=development

# CLIENT_URL: Frontend URL for CORS (update when deploying)
CLIENT_URL=http://localhost:3000

# =============================================================================
# RENDER DEPLOYMENT NOTES
# =============================================================================
# When deploying to Render:
# 1. Set GOOGLE_SHEETS_ID to your Google Sheet ID
# 2. Set GOOGLE_SERVICE_ACCOUNT_EMAIL to your service account email
# 3. Set GOOGLE_PRIVATE_KEY to your service account private key
# 4. Set CLIENT_URL to your frontend Render URL
# 5. PORT will be automatically set by Render
# 6. NODE_ENV will be set to 'production' by Render